# M2 — Racing Profiles and transparent persistence evidence

Status: `in_progress`

## Slice 1 — Create and reopen one Racing Profile

Public test seam: FormationLapCore with a real temporary storage directory.

Observable behavior:

- `CreateProfile` accepts a Racing Profile name and exactly one Primary Sim
  name.
- FormationLapCore assigns a stable identifier.
- `snapshot()` lists the created Racing Profile.
- Reopening FormationLapCore from the same directory returns the same
  identifier, Racing Profile name, and Primary Sim name.

Persistence uses one human-readable JSON document per Racing Profile:

```json
{
  "schemaVersion": 1,
  "id": "<stable UUID>",
  "name": "Le Mans Ultimate",
  "primarySim": {
    "name": "Le Mans Ultimate"
  }
}
```

Creation writes and synchronizes a temporary file in the `profiles` directory,
then renames it to `<profile-id>.json`. Incomplete `.tmp` files are not treated
as Racing Profiles.

### Red

`created_racing_profile_survives_core_restart` initially failed to compile
because `FormationLapCore`, `AppCommand`, `CommandOutcome`, and
`ProfileSummary` did not exist at the approved public seam.

### Green

The same test passed after adding the minimal core and ProfileLibrary behavior:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  created_racing_profile_survives_core_restart

test created_racing_profile_survives_core_restart ... ok
```

## Slice 2 — Reject blank profile names before storage

Public test seam: FormationLapCore with a real temporary storage directory.

Observable behavior:

- A blank or whitespace-only Racing Profile name is rejected.
- A blank or whitespace-only Primary Sim name is rejected.
- Rejected input changes neither the authoritative snapshot nor the profile
  directory.

### Red

`blank_racing_profile_names_are_rejected_without_changing_storage` failed
because `CreateProfile` returned success and persisted the first invalid
profile.

### Green

FormationLapCore now validates both names before delegating to ProfileLibrary:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  blank_racing_profile_names_are_rejected_without_changing_storage -- --exact

test blank_racing_profile_names_are_rejected_without_changing_storage ... ok
```

## Slice 3 — Reject invalid persisted profile names

Public test seam: FormationLapCore with real persisted fixtures.

Observable behavior:

- Opening storage rejects a document with a blank Racing Profile name.
- Opening storage rejects a document with a blank Primary Sim name.
- Invalid persisted documents never enter the authoritative snapshot.

### Red

`persisted_blank_racing_profile_names_are_rejected_on_open` failed because
ProfileLibrary deserialized and accepted both invalid documents.

### Green

ProfileLibrary now applies one name validator to both creation and loading:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles

test blank_racing_profile_names_are_rejected_without_changing_storage ... ok
test created_racing_profile_survives_core_restart ... ok
test persisted_blank_racing_profile_names_are_rejected_on_open ... ok
```

## Slice 4 — Edit and reopen one Racing Profile

Public test seam: FormationLapCore with a real temporary storage directory.

Observable behavior:

- An existing Racing Profile can change its display name and Primary Sim name.
- Editing preserves the Racing Profile's stable identifier.
- The edited values survive FormationLapCore restart.
- The prior valid JSON document is retained in `backups/`.

### Red

`edited_racing_profile_keeps_its_identity_after_restart` initially failed to
compile because FormationLapCore exposed no edit command or update outcome.

### Green

FormationLapCore now delegates edit persistence to ProfileLibrary:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  edited_racing_profile_keeps_its_identity_after_restart -- --exact

test edited_racing_profile_keeps_its_identity_after_restart ... ok
```

## Slice 5 — Delete one Racing Profile

Public test seam: FormationLapCore with a real temporary storage directory.

Observable behavior:

- An existing Racing Profile can be deleted.
- The authoritative snapshot no longer lists it.
- It remains deleted after FormationLapCore restarts.
- The last valid profile document remains in `backups/`.

### Red

`deleted_racing_profile_stays_deleted_and_keeps_a_backup` initially failed to
compile because FormationLapCore exposed no delete command or outcome.

### Green

ProfileLibrary moves the valid document into bounded backup storage before
removing the in-memory profile:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  deleted_racing_profile_stays_deleted_and_keeps_a_backup -- --exact

test deleted_racing_profile_stays_deleted_and_keeps_a_backup ... ok
```

## Slice 6 — Duplicate one Racing Profile

Public test seam: FormationLapCore with a real temporary storage directory.

Observable behavior:

- An existing Racing Profile can be duplicated under a user-supplied name.
- The duplicate receives a different stable identifier.
- The source Racing Profile is unchanged.
- Both profiles survive FormationLapCore restart.

### Red

`duplicated_racing_profile_gets_a_new_identity_that_survives_restart`
initially failed to compile because FormationLapCore exposed no duplicate
command.

### Green

ProfileLibrary copies the source profile data through its ordinary validated
creation path:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  duplicated_racing_profile_gets_a_new_identity_that_survives_restart -- --exact

test duplicated_racing_profile_gets_a_new_identity_that_survives_restart ... ok
```

## Slice 7 — Persist the complete Racing Profile contract

Public test seam: FormationLapCore with a real temporary storage directory.

Observable behavior:

- The Primary Sim has a stable entry identity and a Steam or direct-executable
  Launch Recipe.
- Supporting Applications retain user order, Required/Optional policy,
  keep-running policy, Launch Recipe, timeout, delay, console, elevation, and
  shutdown settings.
- The remembered VR toggle, preferred VR Launch Mode, and Close Session
  SteamVR choice survive restart.
- The full selected Racing Profile is exposed in the authoritative snapshot.
- Rust generates the matching TypeScript contracts.

### Red

`complete_racing_profile_configuration_survives_restart` initially failed to
compile because the complete profile types, save command, and selected profile
snapshot did not exist.

### Green

FormationLapCore now saves the complete contract and ProfileLibrary preserves
it in versioned human-readable JSON:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  complete_racing_profile_configuration_survives_restart -- --exact

test complete_racing_profile_configuration_survives_restart ... ok
```

## Slice 8 — Persist Racing Profile selection

Public test seam: FormationLapCore with a real temporary storage directory.

Observable behavior:

- An existing Racing Profile can be selected by stable identifier.
- The authoritative snapshot exposes the selected profile.
- Selection survives FormationLapCore restart.
- Selection is saved in a versioned, human-readable `settings.json` document
  with a recoverable prior version.

### Red

`selected_racing_profile_survives_core_restart` initially failed to compile
because FormationLapCore exposed no selection command or outcome.

### Green

FormationLapCore validates selection against ProfileLibrary and SettingsStore
persists the chosen identifier:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  selected_racing_profile_survives_core_restart -- --exact

test selected_racing_profile_survives_core_restart ... ok
```

## Slice 9 — Recover an interrupted profile replacement

Public test seam: FormationLapCore with a real temporary storage directory and
an interrupted-write fixture.

Observable behavior:

- If replacement stops after the live document moved to `backups/`, the last
  valid profile is recovered.
- An incomplete temporary document never replaces known-valid state.
- Intentional deletion remains distinct because it leaves no replacement temp
  marker.

### Red

`interrupted_profile_replacement_recovers_the_last_valid_document` reopened
with an empty profile list because ProfileLibrary ignored both the backup and
the interrupted temp file.

### Green

ProfileLibrary now validates and restores the backup before loading profiles:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  interrupted_profile_replacement_recovers_the_last_valid_document -- --exact

test interrupted_profile_replacement_recovers_the_last_valid_document ... ok
```

## Slice 10 — Migrate schema-one profiles

Public test seam: FormationLapCore with a real schema-one profile fixture.

Observable behavior:

- A schema-one minimal profile opens successfully.
- Stable Racing Profile identity and names are preserved.
- Complete schema-two fields receive safe defaults.
- The migrated live document records schema version two.
- The original schema-one document remains in `backups/`.

### Red

`schema_one_profile_is_migrated_without_losing_identity` loaded defaults but
left the live document at schema version one and created no migration backup.

### Green

ProfileLibrary now performs a recoverable on-open migration:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  schema_one_profile_is_migrated_without_losing_identity -- --exact

test schema_one_profile_is_migrated_without_losing_identity ... ok
```

## Dependencies

- `serde_json 1.0.151` serializes the transparent local documents and adds no
  process, network, or privilege capability.
- `uuid 1.24.0` supplies version-four stable identifiers using local randomness
  and adds no process, filesystem, network, update, or privilege capability.
- Both versions were already present transitively in `Cargo.lock`; M2 records
  them as direct dependencies because the core now uses them directly.

## Verification

- `pnpm.cmd verify` passed formatting, ESLint, strict TypeScript, three Vitest
  tests, generated-contract staleness, and the capability audit.
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets
  --all-features -- -D warnings` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed four tests, including
  the new restart-survival behavior test.
- Cargo's documented exFAT incremental-cache hard-link warnings remain
  harmless.

## Next slice

Export one versioned portable Racing Profile document without transient
runtime or process identity.
