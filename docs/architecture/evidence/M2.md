# M2 — Racing Profiles and transparent persistence evidence

Status: `in_progress`

## Slice 1 — Create and reopen one Racing Profile

Public test seam: FormationLapCore with a real temporary storage directory.

Observable behavior:

- `CreateProfile` accepts a Racing Profile name and exactly one Primary Sim
  name.
- FormationLapCore assigns a stable identifier.
- `snapshot()` lists the created Racing Profile.
- Reopening FormationLapCore from the same directory returns the same
  identifier, Racing Profile name, and Primary Sim name.

Persistence uses one human-readable JSON document per Racing Profile:

```json
{
  "schemaVersion": 1,
  "id": "<stable UUID>",
  "name": "Le Mans Ultimate",
  "primarySim": {
    "name": "Le Mans Ultimate"
  }
}
```

Creation writes and synchronizes a temporary file in the `profiles` directory,
then renames it to `<profile-id>.json`. Incomplete `.tmp` files are not treated
as Racing Profiles.

### Red

`created_racing_profile_survives_core_restart` initially failed to compile
because `FormationLapCore`, `AppCommand`, `CommandOutcome`, and
`ProfileSummary` did not exist at the approved public seam.

### Green

The same test passed after adding the minimal core and ProfileLibrary behavior:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  created_racing_profile_survives_core_restart

test created_racing_profile_survives_core_restart ... ok
```

## Slice 2 — Reject blank profile names before storage

Public test seam: FormationLapCore with a real temporary storage directory.

Observable behavior:

- A blank or whitespace-only Racing Profile name is rejected.
- A blank or whitespace-only Primary Sim name is rejected.
- Rejected input changes neither the authoritative snapshot nor the profile
  directory.

### Red

`blank_racing_profile_names_are_rejected_without_changing_storage` failed
because `CreateProfile` returned success and persisted the first invalid
profile.

### Green

FormationLapCore now validates both names before delegating to ProfileLibrary:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles \
  blank_racing_profile_names_are_rejected_without_changing_storage -- --exact

test blank_racing_profile_names_are_rejected_without_changing_storage ... ok
```

## Slice 3 — Reject invalid persisted profile names

Public test seam: FormationLapCore with real persisted fixtures.

Observable behavior:

- Opening storage rejects a document with a blank Racing Profile name.
- Opening storage rejects a document with a blank Primary Sim name.
- Invalid persisted documents never enter the authoritative snapshot.

### Red

`persisted_blank_racing_profile_names_are_rejected_on_open` failed because
ProfileLibrary deserialized and accepted both invalid documents.

### Green

ProfileLibrary now applies one name validator to both creation and loading:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test racing_profiles

test blank_racing_profile_names_are_rejected_without_changing_storage ... ok
test created_racing_profile_survives_core_restart ... ok
test persisted_blank_racing_profile_names_are_rejected_on_open ... ok
```

## Dependencies

- `serde_json 1.0.151` serializes the transparent local documents and adds no
  process, network, or privilege capability.
- `uuid 1.24.0` supplies version-four stable identifiers using local randomness
  and adds no process, filesystem, network, update, or privilege capability.
- Both versions were already present transitively in `Cargo.lock`; M2 records
  them as direct dependencies because the core now uses them directly.

## Verification

- `pnpm.cmd verify` passed formatting, ESLint, strict TypeScript, three Vitest
  tests, generated-contract staleness, and the capability audit.
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets
  --all-features -- -D warnings` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed four tests, including
  the new restart-survival behavior test.
- Cargo's documented exFAT incremental-cache hard-link warnings remain
  harmless.

## Next slice

Edit an existing Racing Profile name and Primary Sim name while preserving its
stable identifier across restart.
