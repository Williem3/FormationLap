{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local",
  "description": "Local-only access for Formation Lap's main window.",
  "local": true,
  "windows": ["main"],
  "permissions": []
}
